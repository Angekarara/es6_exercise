const student = {
  name: 'ange',
  age: 12,
};
const teacher = {
  name: 'muhizi',
  subject: 'Mathing',
};
let text =  `${student.name},
 your age is ${student.age} and,

your teacher is ${teacher.name}.

the subject is ${teacher.subject}`
console.log(text);
 

